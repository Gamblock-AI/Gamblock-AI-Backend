// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/gamblock-ai/gamblock-ai-backend/ent/recoveryspace"
)

// RecoverySpace is the model entity for the RecoverySpace schema.
type RecoverySpace struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID string `json:"user_id,omitempty"`
	// Theme holds the value of the "theme" field.
	Theme recoveryspace.Theme `json:"theme,omitempty"`
	// UnlockedItemsJSON holds the value of the "unlocked_items_json" field.
	UnlockedItemsJSON []string `json:"unlocked_items_json,omitempty"`
	// PlacedItemsJSON holds the value of the "placed_items_json" field.
	PlacedItemsJSON map[string]interface{} `json:"placed_items_json,omitempty"`
	// UnlockRuleVersion holds the value of the "unlock_rule_version" field.
	UnlockRuleVersion int `json:"unlock_rule_version,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RecoverySpace) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case recoveryspace.FieldUnlockedItemsJSON, recoveryspace.FieldPlacedItemsJSON:
			values[i] = new([]byte)
		case recoveryspace.FieldUnlockRuleVersion:
			values[i] = new(sql.NullInt64)
		case recoveryspace.FieldID, recoveryspace.FieldUserID, recoveryspace.FieldTheme:
			values[i] = new(sql.NullString)
		case recoveryspace.FieldCreatedAt, recoveryspace.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RecoverySpace fields.
func (_m *RecoverySpace) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case recoveryspace.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case recoveryspace.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = value.String
			}
		case recoveryspace.FieldTheme:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field theme", values[i])
			} else if value.Valid {
				_m.Theme = recoveryspace.Theme(value.String)
			}
		case recoveryspace.FieldUnlockedItemsJSON:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field unlocked_items_json", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.UnlockedItemsJSON); err != nil {
					return fmt.Errorf("unmarshal field unlocked_items_json: %w", err)
				}
			}
		case recoveryspace.FieldPlacedItemsJSON:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field placed_items_json", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.PlacedItemsJSON); err != nil {
					return fmt.Errorf("unmarshal field placed_items_json: %w", err)
				}
			}
		case recoveryspace.FieldUnlockRuleVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field unlock_rule_version", values[i])
			} else if value.Valid {
				_m.UnlockRuleVersion = int(value.Int64)
			}
		case recoveryspace.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case recoveryspace.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RecoverySpace.
// This includes values selected through modifiers, order, etc.
func (_m *RecoverySpace) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this RecoverySpace.
// Note that you need to call RecoverySpace.Unwrap() before calling this method if this RecoverySpace
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RecoverySpace) Update() *RecoverySpaceUpdateOne {
	return NewRecoverySpaceClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RecoverySpace entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RecoverySpace) Unwrap() *RecoverySpace {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RecoverySpace is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RecoverySpace) String() string {
	var builder strings.Builder
	builder.WriteString("RecoverySpace(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_id=")
	builder.WriteString(_m.UserID)
	builder.WriteString(", ")
	builder.WriteString("theme=")
	builder.WriteString(fmt.Sprintf("%v", _m.Theme))
	builder.WriteString(", ")
	builder.WriteString("unlocked_items_json=")
	builder.WriteString(fmt.Sprintf("%v", _m.UnlockedItemsJSON))
	builder.WriteString(", ")
	builder.WriteString("placed_items_json=")
	builder.WriteString(fmt.Sprintf("%v", _m.PlacedItemsJSON))
	builder.WriteString(", ")
	builder.WriteString("unlock_rule_version=")
	builder.WriteString(fmt.Sprintf("%v", _m.UnlockRuleVersion))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RecoverySpaces is a parsable slice of RecoverySpace.
type RecoverySpaces []*RecoverySpace
