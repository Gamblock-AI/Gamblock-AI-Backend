// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/gamblock-ai/gamblock-ai-backend/ent/recoverypracticesession"
)

// RecoveryPracticeSession is the model entity for the RecoveryPracticeSession schema.
type RecoveryPracticeSession struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID string `json:"user_id,omitempty"`
	// PracticeKind holds the value of the "practice_kind" field.
	PracticeKind recoverypracticesession.PracticeKind `json:"practice_kind,omitempty"`
	// DurationSeconds holds the value of the "duration_seconds" field.
	DurationSeconds int `json:"duration_seconds,omitempty"`
	// Feedback holds the value of the "feedback" field.
	Feedback *recoverypracticesession.Feedback `json:"feedback,omitempty"`
	// CompletedAt holds the value of the "completed_at" field.
	CompletedAt time.Time `json:"completed_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RecoveryPracticeSession) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case recoverypracticesession.FieldDurationSeconds:
			values[i] = new(sql.NullInt64)
		case recoverypracticesession.FieldID, recoverypracticesession.FieldUserID, recoverypracticesession.FieldPracticeKind, recoverypracticesession.FieldFeedback:
			values[i] = new(sql.NullString)
		case recoverypracticesession.FieldCompletedAt, recoverypracticesession.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RecoveryPracticeSession fields.
func (_m *RecoveryPracticeSession) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case recoverypracticesession.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case recoverypracticesession.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = value.String
			}
		case recoverypracticesession.FieldPracticeKind:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field practice_kind", values[i])
			} else if value.Valid {
				_m.PracticeKind = recoverypracticesession.PracticeKind(value.String)
			}
		case recoverypracticesession.FieldDurationSeconds:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field duration_seconds", values[i])
			} else if value.Valid {
				_m.DurationSeconds = int(value.Int64)
			}
		case recoverypracticesession.FieldFeedback:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field feedback", values[i])
			} else if value.Valid {
				_m.Feedback = new(recoverypracticesession.Feedback)
				*_m.Feedback = recoverypracticesession.Feedback(value.String)
			}
		case recoverypracticesession.FieldCompletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field completed_at", values[i])
			} else if value.Valid {
				_m.CompletedAt = value.Time
			}
		case recoverypracticesession.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RecoveryPracticeSession.
// This includes values selected through modifiers, order, etc.
func (_m *RecoveryPracticeSession) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this RecoveryPracticeSession.
// Note that you need to call RecoveryPracticeSession.Unwrap() before calling this method if this RecoveryPracticeSession
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RecoveryPracticeSession) Update() *RecoveryPracticeSessionUpdateOne {
	return NewRecoveryPracticeSessionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RecoveryPracticeSession entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RecoveryPracticeSession) Unwrap() *RecoveryPracticeSession {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RecoveryPracticeSession is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RecoveryPracticeSession) String() string {
	var builder strings.Builder
	builder.WriteString("RecoveryPracticeSession(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_id=")
	builder.WriteString(_m.UserID)
	builder.WriteString(", ")
	builder.WriteString("practice_kind=")
	builder.WriteString(fmt.Sprintf("%v", _m.PracticeKind))
	builder.WriteString(", ")
	builder.WriteString("duration_seconds=")
	builder.WriteString(fmt.Sprintf("%v", _m.DurationSeconds))
	builder.WriteString(", ")
	if v := _m.Feedback; v != nil {
		builder.WriteString("feedback=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("completed_at=")
	builder.WriteString(_m.CompletedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RecoveryPracticeSessions is a parsable slice of RecoveryPracticeSession.
type RecoveryPracticeSessions []*RecoveryPracticeSession
