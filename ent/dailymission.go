// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/gamblock-ai/gamblock-ai-backend/ent/dailymission"
)

// DailyMission is the model entity for the DailyMission schema.
type DailyMission struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID string `json:"user_id,omitempty"`
	// MissionDate holds the value of the "mission_date" field.
	MissionDate *string `json:"mission_date,omitempty"`
	// MissionKey holds the value of the "mission_key" field.
	MissionKey string `json:"mission_key,omitempty"`
	// Status holds the value of the "status" field.
	Status dailymission.Status `json:"status,omitempty"`
	// ExpReward holds the value of the "exp_reward" field.
	ExpReward int `json:"exp_reward,omitempty"`
	// CompletedAt holds the value of the "completed_at" field.
	CompletedAt *time.Time `json:"completed_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DailyMission) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case dailymission.FieldExpReward:
			values[i] = new(sql.NullInt64)
		case dailymission.FieldID, dailymission.FieldUserID, dailymission.FieldMissionDate, dailymission.FieldMissionKey, dailymission.FieldStatus:
			values[i] = new(sql.NullString)
		case dailymission.FieldCompletedAt, dailymission.FieldCreatedAt, dailymission.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DailyMission fields.
func (_m *DailyMission) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case dailymission.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case dailymission.FieldUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				_m.UserID = value.String
			}
		case dailymission.FieldMissionDate:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field mission_date", values[i])
			} else if value.Valid {
				_m.MissionDate = new(string)
				*_m.MissionDate = value.String
			}
		case dailymission.FieldMissionKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field mission_key", values[i])
			} else if value.Valid {
				_m.MissionKey = value.String
			}
		case dailymission.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = dailymission.Status(value.String)
			}
		case dailymission.FieldExpReward:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field exp_reward", values[i])
			} else if value.Valid {
				_m.ExpReward = int(value.Int64)
			}
		case dailymission.FieldCompletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field completed_at", values[i])
			} else if value.Valid {
				_m.CompletedAt = new(time.Time)
				*_m.CompletedAt = value.Time
			}
		case dailymission.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case dailymission.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DailyMission.
// This includes values selected through modifiers, order, etc.
func (_m *DailyMission) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this DailyMission.
// Note that you need to call DailyMission.Unwrap() before calling this method if this DailyMission
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DailyMission) Update() *DailyMissionUpdateOne {
	return NewDailyMissionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DailyMission entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DailyMission) Unwrap() *DailyMission {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: DailyMission is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DailyMission) String() string {
	var builder strings.Builder
	builder.WriteString("DailyMission(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_id=")
	builder.WriteString(_m.UserID)
	builder.WriteString(", ")
	if v := _m.MissionDate; v != nil {
		builder.WriteString("mission_date=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("mission_key=")
	builder.WriteString(_m.MissionKey)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("exp_reward=")
	builder.WriteString(fmt.Sprintf("%v", _m.ExpReward))
	builder.WriteString(", ")
	if v := _m.CompletedAt; v != nil {
		builder.WriteString("completed_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DailyMissions is a parsable slice of DailyMission.
type DailyMissions []*DailyMission
